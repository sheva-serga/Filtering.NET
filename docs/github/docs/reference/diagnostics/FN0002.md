---
title: FN0002
description: Property is marked `Sortable = true` on multiple `[Map]` methods.
---

# FN0002 — Duplicate sortable mapping

**Severity:** Error

## What triggers it

The same property is marked `Sortable = true` on more than one `[Map]` declaration on the same filter class.

## Why it matters

Sort dispatch goes through a single `switch` on field-name in `ApplySorting`; duplicates would produce overlapping case labels.

## Fix

Keep `Sortable = true` on exactly one of the duplicate mappings, or eliminate the duplicate (see also `FN0001`).

## See also

- [Sortable properties](../../guides/sortable-properties.md)
- [Diagnostics catalogue](index.md)
- [View descriptor in source](https://github.com/sheva-serga/Filtering.NET/blob/main/src/Filtering.Net.Generator/Diagnostics/DiagnosticDescriptors.cs)
