---
title: FN0014
description: `[InterceptValue]` declared without a matching `[Map]` for the property.
---

# FN0014 — Interceptor declared without matching `[Map]`

**Severity:** Error

## What triggers it

A method has `[InterceptValue("Name")]` but no `[Map(nameof(User.Name))]` is declared on the same filter class.

## Why it matters

The interceptor only runs as part of the leaf-value extraction step for its target property. Without a corresponding `[Map]` there's nothing to attach the interceptor to.

## Fix

Add the matching `[Map]`, or remove the orphan interceptor.

## See also

- [Intercepting values](../../guides/intercepting-values.md)
- [Diagnostics catalogue](index.md)
- [View descriptor in source](https://github.com/sheva-serga/Filtering.NET/blob/main/src/Filtering.Net.Generator/Diagnostics/DiagnosticDescriptors.cs)
